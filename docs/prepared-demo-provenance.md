# Prepared Demo provenance

Status: **not created**.

The prepared demo may exist only after a documented successful production-pipeline execution. It must record source rights, source hash, processing timestamp, pipeline/prompt versions, Gemini source/model/window provenance, fallback or escalation calls, selected Gloo endpoint/decisions, YouVersion references/attribution, and final Echo IDs.

The application may use a prepared result only for this exact labelled source. It can never answer a newly submitted source or satisfy either real acceptance gate.
