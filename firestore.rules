rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients use the authenticated FastAPI boundary. Cloud Run's
    // Admin SDK bypasses these rules; no project or provenance data is exposed
    // by a direct Firestore client path.
    match /{document=**} { allow read, write: if false; }
  }
}
